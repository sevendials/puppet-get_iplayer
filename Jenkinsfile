pipeline {
    agent { docker 'ruby:2.2.8' } 
    stages {
        stage('Example Build') {
            steps {
                sh 'which ruby'
                sh 'pwd'

            }
        }
    }
}
